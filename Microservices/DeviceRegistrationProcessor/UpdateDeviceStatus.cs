using Microsoft.Azure.WebJobs;
using Microsoft.Extensions.Logging;
using System;

namespace DeviceRegistrationProcessor
{
    public static class UpdateDeviceStatus
    {
        
        //public static void Run([ServiceBusTrigger("deviceregistrationtopic", "readprojection", Connection = "ServiceBus")] string mySbMsg, ILogger log)
        //{
        //    log.LogInformation($"C# ServiceBus topic trigger function processed message: {mySbMsg}");
        //}
    }
}
